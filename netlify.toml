# No build command on purpose: this is a single static file.
# A build command would spin up a build container on every push and burn
# build-minute credits. We stamp the commit hash ourselves before pushing,
# so Netlify just uploads index.html (no build = ~0 build minutes).
[build]
  publish = "."
